```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <title>For Marina ❤️</title>

    <style>
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            min-height: 100vh;
            font-family: Arial, sans-serif;
            background: linear-gradient(135deg, #fff0f5, #ffe4ec);
            color: #4a2632;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 25px;
        }

        .container {
            width: 100%;
            max-width: 700px;
            text-align: center;
        }

        .heart {
            font-size: 70px;
            animation: heartbeat 1.5s infinite;
            margin-bottom: 20px;
        }

        h1 {
            font-size: 44px;
            margin-bottom: 15px;
        }

        .subtitle {
            font-size: 19px;
            line-height: 1.6;
            margin-bottom: 35px;
        }

        .card {
            background: rgba(255, 255, 255, 0.85);
            border-radius: 25px;
            padding: 30px;
            box-shadow: 0 10px 30px rgba(100, 40, 60, 0.15);
            margin-bottom: 25px;
        }

        .card h2 {
            margin-bottom: 15px;
            font-size: 27px;
        }

        .card p {
            font-size: 17px;
            line-height: 1.7;
        }

        .story-button {
            display: inline-block;
            text-decoration: none;
            padding: 16px 30px;
            border-radius: 30px;
            font-size: 17px;
            background: #ff6f91;
            color: white;
            transition: 0.3s;
            box-shadow: 0 6px 18px rgba(255, 111, 145, 0.3);
        }

        .story-button:hover {
            transform: scale(1.05);
        }

        footer {
            margin-top: 30px;
            font-size: 14px;
            opacity: 0.7;
        }

        @keyframes heartbeat {
            0%, 100% {
                transform: scale(1);
            }

            50% {
                transform: scale(1.15);
            }
        }
    </style>
</head>

<body>

    <div class="container">

        <div class="heart">❤️</div>

        <h1>For Marina</h1>

        <p class="subtitle">
            A little place on the internet<br>
            made especially for you.
        </p>

        <div class="card">

            <h2>🌸 Our little story</h2>

            <p>
                Sometimes people appear in our lives
                unexpectedly and become very special.
                This website is a small place
                for our memories.
            </p>

        </div>

        <a href="story.html" class="story-button">
            Open our story ❤️
        </a>

        <footer>
            Made with love ❤️
        </footer>

    </div>

</body>
</html>
```

